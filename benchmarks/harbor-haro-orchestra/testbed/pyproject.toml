[project]
name = "harbor-haro-testbed"
version = "0.1.0"
description = "Trial provisioning and local Haro stack tooling for harbor-haro-orchestra"
requires-python = ">=3.12"
dependencies = [
    "harbor-haro-orchestra",
    "coincurve>=20",
    "psycopg[binary]>=3.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.optional-dependencies]
dev = [
    "pytest>=8.4",
    "ruff>=0.15",
]

[tool.uv.sources]
harbor-haro-orchestra = { path = "..", editable = true }

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
target-version = "py312"
line-length = 88
