{{- include "haro.validate" . -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "haro.fullname" . }}
  labels:
    {{- include "haro.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      {{- include "haro.relaySelectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "haro.relaySelectorLabels" . | nindent 8 }}
        {{- with .Values.relay.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      annotations:
        # Roll pods when the chart-managed Secret changes.
        checksum/secret: {{ include (print $.Template.BasePath "/secret-chart.yaml") . | sha256sum }}
        {{- with .Values.relay.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      serviceAccountName: {{ include "haro.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.relay.securityContext | nindent 8 }}
      terminationGracePeriodSeconds: {{ .Values.relay.terminationGracePeriodSeconds }}
      {{- with .Values.image.pullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.relay.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.relay.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.relay.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.relay.topologySpreadConstraints }}
      topologySpreadConstraints:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.minio.enabled }}
      # Quickstart only: the bundled MinIO bucket is created by a concurrent
      # init Job (templates/quickstart-minio-init.yaml). The relay's A3 S3
      # conformance probe is startup-fatal, so without this gate the relay Pods
      # CrashLoopBackOff (with growing backoff) until the bucket appears. Block
      # relay start until the bucket exists — deterministic, no crash-loops.
      initContainers:
        - name: wait-for-bucket
          image: {{ .Values.minio.mcImage | quote }}
          securityContext:
            {{- toYaml .Values.relay.containerSecurityContext | nindent 12 }}
          env:
            - name: S3_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "haro.chartSecretName" . }}
                  key: HARO_S3_ACCESS_KEY
            - name: S3_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "haro.chartSecretName" . }}
                  key: HARO_S3_SECRET_KEY
          command: ["/bin/sh", "-c"]
          args:
            - |
              set -e
              export MC_CONFIG_DIR=/tmp/.mc
              until mc alias set local {{ include "haro.minioEndpoint" . }} "$S3_ACCESS_KEY" "$S3_SECRET_KEY" >/dev/null 2>&1; do
                echo "waiting for MinIO..."; sleep 2
              done
              until mc stat local/{{ .Values.s3.bucket }} >/dev/null 2>&1; do
                echo "waiting for bucket {{ .Values.s3.bucket }}..."; sleep 2
              done
              echo "bucket {{ .Values.s3.bucket }} present"
      {{- end }}
      containers:
        - name: relay
          image: {{ include "haro.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            {{- toYaml .Values.relay.containerSecurityContext | nindent 12 }}
          ports:
            - { name: app,     containerPort: 3000, protocol: TCP }
            - { name: health,  containerPort: {{ .Values.service.healthPort }}, protocol: TCP }
            - { name: metrics, containerPort: {{ .Values.service.metricsPort }}, protocol: TCP }
          env:
            # ── Networking ───────────────────────────────────────────
            - { name: HARO_BIND_ADDR,     value: {{ .Values.relay.bindAddr | quote }} }
            - { name: HARO_HEALTH_PORT,   value: {{ .Values.service.healthPort | quote }} }
            - { name: HARO_METRICS_PORT,  value: {{ .Values.service.metricsPort | quote }} }
            - { name: RELAY_URL,          value: {{ .Values.relayUrl | quote }} }
            {{- if .Values.pairingRelay.url }}
            - { name: HARO_PAIRING_RELAY_URL, value: {{ .Values.pairingRelay.url | quote }} }
            {{- end }}
            - { name: HARO_MEDIA_BASE_URL, value: {{ include "haro.mediaBaseUrl" . | quote }} }

            # ── Behavior ─────────────────────────────────────────────
            - { name: HARO_MAX_CONNECTIONS,        value: {{ .Values.relay.maxConnections | quote }} }
            - { name: HARO_MAX_CONCURRENT_HANDLERS, value: {{ .Values.relay.maxConcurrentHandlers | quote }} }
            - { name: HARO_SEND_BUFFER,            value: {{ .Values.relay.sendBuffer | quote }} }
            - { name: HARO_REQUIRE_AUTH_TOKEN,     value: {{ .Values.relay.requireAuthToken | quote }} }
            - { name: HARO_REQUIRE_RELAY_MEMBERSHIP, value: {{ .Values.relay.requireRelayMembership | quote }} }
            - { name: HARO_REQUIRE_MEDIA_GET_AUTH, value: {{ .Values.relay.requireMediaGetAuth | quote }} }
            - { name: HARO_ALLOW_NIP_OA_AUTH,      value: {{ .Values.relay.allowNipOaAuth | quote }} }
            - { name: HARO_PUBKEY_ALLOWLIST,       value: {{ .Values.relay.pubkeyAllowlist | quote }} }
            {{- if .Values.relay.corsOrigins }}
            - { name: HARO_CORS_ORIGINS, value: {{ join "," .Values.relay.corsOrigins | quote }} }
            {{- end }}
            {{- if gt (.Values.relay.ephemeralTtlOverride | int) 0 }}
            - { name: HARO_EPHEMERAL_TTL_OVERRIDE, value: {{ .Values.relay.ephemeralTtlOverride | quote }} }
            {{- end }}
            {{- if .Values.relay.uploadRecords }}
            - { name: HARO_MEDIA_UPLOAD_RECORDS, value: "true" }
            {{- end }}
            {{- if .Values.relay.uploadIpHeader }}
            - { name: HARO_MEDIA_UPLOAD_IP_HEADER, value: {{ .Values.relay.uploadIpHeader | quote }} }
            {{- end }}
            {{- if .Values.relay.uploadPortHeader }}
            - { name: HARO_MEDIA_UPLOAD_PORT_HEADER, value: {{ .Values.relay.uploadPortHeader | quote }} }
            {{- end }}

            # ── Owner ────────────────────────────────────────────────
            - { name: RELAY_OWNER_PUBKEY, value: {{ .Values.ownerPubkey | quote }} }

            # ── Migrations ───────────────────────────────────────────
            - { name: HARO_AUTO_MIGRATE, value: {{ .Values.migrate.autoMigrate | quote }} }

            # ── Git ──────────────────────────────────────────────────
            - { name: HARO_GIT_REPO_PATH,         value: {{ .Values.persistence.git.mountPath | quote }} }
            - { name: HARO_GIT_MAX_PACK_BYTES,    value: {{ .Values.git.maxPackBytes | quote }} }
            - { name: HARO_GIT_PACK_CACHE_PATH,   value: {{ .Values.git.packCachePath | quote }} }
            - { name: HARO_GIT_PACK_CACHE_MAX_BYTES, value: {{ .Values.git.packCacheMaxBytes | quote }} }
            - { name: HARO_GIT_PACK_CACHE_MAX_CONCURRENT_POPULATIONS, value: {{ .Values.git.packCacheMaxConcurrentPopulations | quote }} }
            - { name: HARO_GIT_MAX_REPOS_PER_PUBKEY, value: {{ .Values.git.maxReposPerPubkey | quote }} }
            - { name: HARO_GIT_MAX_CONCURRENT_OPS, value: {{ .Values.git.maxConcurrentOps | quote }} }

            # ── S3 (non-secret) ──────────────────────────────────────
            {{- $s3Endpoint := include "haro.s3Endpoint" . }}
            {{- if $s3Endpoint }}
            - { name: HARO_S3_ENDPOINT, value: {{ $s3Endpoint | quote }} }
            {{- end }}
            - { name: HARO_S3_BUCKET, value: {{ .Values.s3.bucket | quote }} }

            # ── Secrets (from chart-managed or existing) ─────────────
            - name: HARO_RELAY_PRIVATE_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "haro.envSecretName" . }}
                  key: HARO_RELAY_PRIVATE_KEY
                  optional: true
            - name: HARO_GIT_HOOK_HMAC_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "haro.envSecretName" . }}
                  key: HARO_GIT_HOOK_HMAC_SECRET
                  optional: true
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "haro.envSecretName" . }}
                  key: DATABASE_URL
            - name: READ_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "haro.envSecretName" . }}
                  key: READ_DATABASE_URL
                  optional: true
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "haro.envSecretName" . }}
                  key: REDIS_URL
                  optional: {{ and (eq (include "haro.minimumReplicas" . | int) 1) (not .Values.redis.enabled) (not .Values.externalRedis.url) }}
            - name: HARO_S3_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "haro.envSecretName" . }}
                  key: HARO_S3_ACCESS_KEY
                  optional: true
            - name: HARO_S3_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "haro.envSecretName" . }}
                  key: HARO_S3_SECRET_KEY
                  optional: true

            - name: HARO_HUDDLE_AUDIO_AVAILABLE
              value: {{ include "haro.huddleAudioAvailable" . | quote }}

            {{- with .Values.relay.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- with .Values.relay.extraEnvFrom }}
          envFrom:
            {{- toYaml . | nindent 12 }}
          {{- end }}

          livenessProbe:
            {{- toYaml .Values.relay.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.relay.readinessProbe | nindent 12 }}
          startupProbe:
            {{- toYaml .Values.relay.startupProbe | nindent 12 }}

          resources:
            {{- toYaml .Values.relay.resources | nindent 12 }}

          volumeMounts:
            - { name: git-repos, mountPath: {{ .Values.persistence.git.mountPath | quote }} }
            - { name: git-pack-cache, mountPath: {{ .Values.git.packCachePath | quote }} }

      volumes:
        - name: git-repos
        {{- if .Values.persistence.git.enabled }}
          persistentVolumeClaim:
            claimName: {{ default (printf "%s-git" (include "haro.fullname" .)) .Values.persistence.git.existingClaim }}
        {{- else }}
          emptyDir:
            sizeLimit: {{ .Values.persistence.git.size | quote }}
        {{- end }}
        - name: git-pack-cache
          emptyDir:
            sizeLimit: {{ .Values.git.packCacheVolumeSize | quote }}
